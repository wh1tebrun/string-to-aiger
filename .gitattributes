# Auto detect text files and perform LF normalization
* text=auto

# Keep Bash/WSL/Docker entry scripts executable after Windows checkouts.
*.sh text eol=lf

# AIGER tools require LF in line-oriented models and stimuli.
*.aag text eol=lf
*.stim text eol=lf
